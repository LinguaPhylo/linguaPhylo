data {
  L = 200;
  taxa = taxa(names=1:10);
}
model {
  Θ ~ LogNormal(meanlog=3.0, sdlog=1.0);
  ψ ~ Coalescent(theta=Θ, taxa=taxa);

  a ~ Normal(mean = 0.001, sd = 0.1);
  b ~ Normal(mean = 0.0, sd = 0.01);
  μ = exp(a + b);

  D ~ PhyloCTMC(tree=ψ, L=L, Q=jukesCantor(), mu=μ);
}
