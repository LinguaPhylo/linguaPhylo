name: Lphy tests
on: [ push, pull_request, workflow_dispatch ]

jobs:
  test-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker build -t lphy-test .
      - run: docker run lphy-test github-actions
